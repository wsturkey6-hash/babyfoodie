# babyfoodie 規格

> 2026-09-19 與使用者確認。試敏表食材清單仍待核對（見文末「待確認」）。

## 基本決定

| 項目 | 決定 |
|---|---|
| 技術 | 純 HTML / CSS / JavaScript，不用框架、不需要建置 |
| 部署 | GitHub Pages（公開 repo） |
| 資料儲存 | 存在瀏覽器本機（localStorage），可匯出／匯入備份檔 |
| 介面語言 | 繁體中文 |
| 裝置 | 手機為主，電腦也能用（響應式） |
| 設計風格 | B 貼紙手帳風（見「設計」） |

## 頁面

三個頁面，用底部分頁列切換：**紀錄／試敏表／寶寶**。

### 1. 紀錄

每筆紀錄的欄位：

| 欄位 | 說明 |
|---|---|
| 日期 | |
| 時間 | |
| 副食品內容 | 從試敏表的食材挑選，可複選；表上沒有的可以自己新增（見「自訂食材」） |
| 份量 | 數字 + 單位，單位用選的：匙、ml、g |
| 觀察記錄 | 常見反應勾選 + 文字備註 |

- 儲存紀錄後，裡面的食材在試敏表自動變成「已嘗試」。
- 觀察記錄有勾任何反應時，儲存後會詢問要把哪些食材標成「有反應」（預設勾選這次第一次吃的食材）。

### 2. 試敏表

- 依月齡分區：4、5、6、7、8、9、10 個月，最後是「其他」（自訂食材）。
- 每個食材顯示建議嘗試順序編號和名稱。所有食材樣式相同（原圖的粗體沒有特別意思）。
- 三種狀態，用顏色加圖示區分，不只靠顏色：

  | 狀態 | 外觀 |
  |---|---|
  | 未嘗試 | 白底 |
  | 已嘗試 | 綠底 + 勾勾 |
  | 有反應 | 紅底 + 警示圖示 |

- 狀態來源：紀錄自動標記「已嘗試」；也可以直接點食材手動修改（例如開始用網站前就吃過的）。
- 依寶寶目前月齡，標示對應的那一區。

### 3. 寶寶

- 輸入寶寶名字、生日。
- 顯示目前月齡，格式「X 個月 Y 天」。

### 自訂食材

- 新增紀錄時，可以輸入試敏表上沒有的食材。
- 自訂食材會加到試敏表的「其他」區，一樣有三種狀態。

## 預設做法

以下細節沒有特別討論，先照這樣做，有意見隨時改：

- 新增紀錄時，日期和時間預設為現在。
- 常見反應選項：紅疹、腹瀉、嘔吐、便秘、脹氣。
- 紀錄列表依日期時間由新到舊排列；紀錄可編輯、刪除（刪除前會確認）。
- 紀錄卡片上，第一次吃的食材標「第一次」（設計選項 B 畫面裡的樣子）。
- 刪除紀錄後，若某食材已沒有任何紀錄、也沒手動標記過，會變回「未嘗試」。
- 自訂食材沒有順序編號，依新增先後排列。
- 月齡標示：未滿 4 個月不標示任何一區；10 個月以上標示 10 個月那一區。
- 匯出／匯入放在「寶寶」頁。匯出成一個 JSON 檔；匯入會覆蓋目前資料（匯入前會確認）。

## 設計

參考畫面：[design/options.html](design/options.html) 中的 B。

- 風格：貼紙手帳風（ui-ux-pro-max 的 Neubrutalism，改成圓角 + 暖色）
- 字型：粉圓 Huninn（Google Fonts）。只有一種粗細，層次用字級區分
- 形狀：2～2.5px 深可可色框線、硬陰影 `4px 4px 0`、圓角 12～18px、點點紙紋背景
- 互動：按下時往陰影方向位移（只動 transform，0.12 秒 ease-out）；尊重「減少動態效果」設定
- 配色：

  | 用途 | 色碼 |
  |---|---|
  | 紙張背景 | `#FFF6E5` |
  | 卡片 | `#FFFFFF` |
  | 墨色（文字、框線、陰影） | `#3B2418` |
  | 次要文字 | `#7A5A48` |
  | 主要按鈕（橘） | `#FF8A4C` |
  | 強調（芥末黃） | `#FFC93C` |
  | 已嘗試（綠） | `#8FD17F` |
  | 有反應（番茄紅） | `#FF6B61` |

  以上文字與底色組合的對比度都 ≥ 4.5:1。

## 試敏表食材清單

來源：使用者提供的圖片。格式為「編號 名稱」；標 `?` 的是看不清楚、需要核對的地方。

**4 個月**：1 十倍粥、2 米湯、3 小米粥、4 藜麥、5 紅藜、6 紅蘿蔔、7 花椰菜、8 高麗菜、9 南瓜、10 地瓜、11 地瓜葉、12 青江菜、13 小松菜、14 油菜、15 蘋果、16 梨子、17 莧菜、18 大白菜、19 小白菜、20 大陸妹、21 美生菜、22 芥蘭菜、23 空心菜、24 冬瓜、25 火龍果、26 白蘿蔔、27 西洋梨、28 豆薯、29? 甜椒、29? 青菜（或青椒?）、29? 葡萄、29? 馬鈴薯、29? 洋蔥、29? 酪梨、30? 香蕉

**5 個月**：30 玉米筍、31 玉米、32 黑木耳、33 蘆筍、33? 柿子、34? 皇宮菜、34? 豌豆苗、34? 蓮霧（或蓮藕?）、35 綠豆芽

**6 個月**：36 蔬菜高湯、37 青蔥、38 韭菜、39 大蒜、40 木瓜、41 甜菜根、42 米豆、43 芭樂、43? 菠菜、44? 香瓜、44? 蜜李、45? 茭白筍、45? 茄子、46 蕃茄

**7 個月**：48 糙米粥、49 菱角、49 草菇、50 毛豆、51 皇帝豆、51 秋葵、52 芹菜、52 山芹菜、53 韭黃、53? 韭菜?、54 豌豆仁、55 絲瓜、56 苦瓜、57 瓠瓜、57 大黃瓜、58 小黃瓜、59 雞肉、60 魚肉、61 豬肉、62 牛肉、63 豆腐、64 蛋黃、65 雞高湯、66 魚高湯、66 豬高湯、67 蒜頭雞湯

**8 個月**：68 紅豆粥、69 桂圓粥、70 紫米粥、71 黑米粥、72 燕麥粥、72? 紅藜粥（或紅棗粥?）、73 蓮子粥、73? 蓮藕、74 山藥、75 黑芝麻、76 白木耳、77? 枸杞

**9 個月**：79 海帶芽粥、79? 昆布高湯、79? 蘑菇高湯、80 蘑菇、81 鮮香菇、82 金針菇、83 杏鮑菇、83? 鴻喜菇、84 起司

**10 個月**：87? 核桃粥、88 雪蓮子粥、88? 百合粥、89 芋頭、90 栗子、91 竹筍、92 牛蒡、92? 蝦子、93 鳳梨、94 芒果、94? 水蜜桃、95 檸檬、95? 奇異果、96 柳丁

### 待確認

1. 圖片解析度不高，**同一個編號出現好幾次的地方**特別沒把握，請對照原圖核對所有標 `?` 的編號。
2. 4 個月的「青菜」可能是「青椒」；5 個月的「蓮霧」可能是「蓮藕」；8 個月的「紅藜粥」可能是「紅棗粥」。
3. 7 個月「韭黃」下一項讀起來像「韭菜」，但 6 個月已經有韭菜，可能讀錯。
4. 沒看到編號 47、78、85、86：是本來就沒有、我讀錯，還是圖片被裁到？4 個月那欄貼齊圖片底部，下面可能還有項目。
