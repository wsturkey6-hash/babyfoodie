# babyfoodie 規格

> 2026-09-19 與使用者確認，包含試敏表食材清單。

## 基本決定

| 項目 | 決定 |
|---|---|
| 技術 | 純 HTML / CSS / JavaScript，不用框架、不需要建置 |
| 部署 | GitHub Pages（公開 repo） |
| 資料儲存 | 存在瀏覽器本機（localStorage），可匯出／匯入備份檔 |
| 介面語言 | 繁體中文 |
| 裝置 | 手機為主；電腦上跟手機一樣是單欄，置中顯示，最寬 480px |
| 設計風格 | B 貼紙手帳風（見「設計」） |

## 頁面

三個頁面，用底部分頁列切換：**紀錄／試敏表／寶寶**。

- 每一頁的頁首都顯示寶寶名字和月齡。
- 還沒設定寶寶資料時，頁首顯示「設定寶寶資料」按鈕，點了才去寶寶頁（不強迫跳轉）。

### 1. 紀錄

每筆紀錄的欄位：

| 欄位 | 說明 |
|---|---|
| 日期 | |
| 時間 | |
| 副食品內容 | 從試敏表的食材挑選，可複選；表上沒有的可以自己新增（見「自訂食材」） |
| 份量 | 數字 + 單位，單位用選的：匙、ml、g |
| 觀察記錄 | 常見反應勾選 + 文字備註 |

- 儲存紀錄後，裡面的食材在試敏表自動變成「已嘗試」。
- 觀察記錄有勾任何反應時，儲存後會詢問要把哪些食材標成「有反應」（預設勾選這次第一次吃的食材）。

### 2. 試敏表

- 依月齡分區：4、5、6、7、8、9、10 個月，最後是「其他」（自訂食材）。
- 每個食材顯示建議嘗試順序編號和名稱。所有食材樣式相同（原圖的粗體沒有特別意思）。
- 同一個月內依編號由小到大排列；同編號的照原圖先後。
- 三種狀態，用顏色加圖示區分，不只靠顏色：

  | 狀態 | 外觀 |
  |---|---|
  | 未嘗試 | 白底 |
  | 已嘗試 | 綠底 + 勾勾 |
  | 有反應 | 紅底 + 警示圖示 |

- 狀態來源：紀錄自動標記「已嘗試」；也可以直接點食材手動修改（例如開始用網站前就吃過的）。
- 依寶寶目前月齡，標示對應的那一區。

### 3. 寶寶

- 輸入寶寶名字、生日（不能晚於今天）。
- 顯示目前月齡，格式「X 個月 Y 天」。
- 按「儲存」才會存，存好顯示「已儲存」；還沒儲存就離開這頁時會先提醒。
- 頁首的寶寶名字和月齡可以點，點了進寶寶頁修改（寶寶頁自己的頁首不能點）。

### 自訂食材

- 新增紀錄時，可以輸入試敏表上沒有的食材。
- 自訂食材會加到試敏表的「其他」區，一樣有三種狀態。

## 預設做法

以下細節沒有特別討論，先照這樣做，有意見隨時改：

- 新增紀錄時，日期和時間預設為現在。
- 常見反應選項：紅疹、腹瀉、嘔吐、便秘、脹氣。
- 紀錄列表依日期時間由新到舊排列；紀錄可編輯、刪除（刪除前會確認）。
- 紀錄卡片上，第一次吃的食材標「第一次」（設計選項 B 畫面裡的樣子）。
- 刪除紀錄後，若某食材已沒有任何紀錄、也沒手動標記過，會變回「未嘗試」。
- 自訂食材沒有順序編號，依新增先後排列。
- 月齡標示：未滿 4 個月不標示任何一區；10 個月以上標示 10 個月那一區。
- 寶寶名字最多 20 字。
- 月齡計算：生日是 31 號這類某些月份沒有的日子時，那個月以最後一天當作滿月日（例如 1/31 出生，2/28 滿 1 個月）。
- 匯出／匯入放在「寶寶」頁。匯出成一個 JSON 檔；匯入會覆蓋目前資料（匯入前會確認）。

## 設計

參考畫面：[design/options.html](design/options.html) 中的 B。

- 風格：貼紙手帳風（ui-ux-pro-max 的 Neubrutalism，改成圓角 + 暖色）
- 字型：粉圓 Huninn（Google Fonts）。只有一種粗細，層次用字級區分
- 形狀：2～2.5px 深可可色框線、硬陰影 `4px 4px 0`、圓角 12～18px、點點紙紋背景
- 互動：按下時往陰影方向位移（只動 transform，0.12 秒 ease-out）；尊重「減少動態效果」設定
- 配色：

  | 用途 | 色碼 |
  |---|---|
  | 紙張背景 | `#FFF6E5` |
  | 卡片 | `#FFFFFF` |
  | 墨色（文字、框線、陰影） | `#3B2418` |
  | 次要文字 | `#7A5A48` |
  | 主要按鈕（橘） | `#FF8A4C` |
  | 強調（芥末黃） | `#FFC93C` |
  | 已嘗試（綠） | `#8FD17F` |
  | 有反應（番茄紅） | `#FF6B61` |

  以上文字與底色組合的對比度都 ≥ 4.5:1。

## 試敏表食材清單

來源：使用者提供的圖片，2026-09-19 逐月核對過，共 118 項。格式為「編號 名稱」，已依編號排好（即試敏表上的顯示順序）。

- 編號本來就有重複，也本來就跳過 47、78、85、86。
- 原圖 4 個月的「青菜」是籠統名稱，已刪除。

**4 個月（34 項）**：1 十倍粥、2 米湯、3 小米粥、4 藜麥、5 紅藜、6 紅蘿蔔、7 花椰菜、8 高麗菜、9 南瓜、10 地瓜、11 地瓜葉、12 青江菜、13 小松菜、14 油菜、15 蘋果、16 梨子、17 莧菜、18 大白菜、19 小白菜、20 大陸妹、21 美生菜、22 芥蘭菜、23 空心菜、24 冬瓜、24 甜椒、25 火龍果、26 白蘿蔔、26 葡萄、26 馬鈴薯、27 西洋梨、28 豆薯、28 洋蔥、29 酪梨、30 香蕉

**5 個月（9 項）**：30 玉米筍、31 玉米、32 黑木耳、32 蘆筍、33 柿子、33 皇宮菜、34 豌豆苗、34 蓮霧、35 綠豆芽

**6 個月（14 項）**：36 蔬菜高湯、37 青蔥、38 薑、39 大蒜、40 木瓜、41 甜菜根、42 米豆、42 芭樂、43 菠菜、43 香瓜、44 蜜李、44 茭白筍、45 茄子、46 蕃茄

**7 個月（26 項）**：48 糙米粥、49 菱角、49 草莓、50 毛豆、51 皇帝豆、51 秋葵、52 芹菜、52 山芹菜、53 韭黃、53 韭菜、54 豌豆仁、55 絲瓜、56 苦瓜、57 瓠瓜、57 大黃瓜、58 小黃瓜、59 雞肉、60 魚肉、61 豬肉、62 牛肉、63 豆腐、64 蛋黃、65 雞高湯、66 魚高湯、66 豬高湯、67 蒜頭雞湯

**8 個月（12 項）**：68 紅豆粥、69 桂圓粥、70 紫米粥、71 黑米粥、72 燕麥粥、72 紅棗粥、73 蓮子粥、73 蓮藕、74 山藥、75 黑芝麻、76 白木耳、77 枸杞

**9 個月（9 項）**：79 海帶芽粥、79 昆布高湯、79 蘑菇高湯、80 蘑菇、81 鮮香菇、82 金針菇、83 杏鮑菇、83 鴻喜菇、84 起司

**10 個月（14 項）**：87 核桃粥、88 雪蓮子粥、88 百合粥、89 芋頭、90 栗子、91 竹筍、92 牛蒡、92 蝦子、93 鳳梨、94 芒果、94 水蜜桃、95 檸檬、95 奇異果、96 柳丁
